{
  "name": "rule-eight-2",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.7.0",
    "supervisor": "^0.12.0"
  },
  "dependencies": {
    "express": "^4.14.0",
    "react": "^15.4.0",
    "react-dom": "^15.4.0",
    "superagent": "^2.3.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "start-proxy": "node src/proxy.js",
    "prod-start": "react-scripts build && npm run start-proxy & pushstate-server build &",
    "deploy": "sh scripts/deploy.sh"
  }
}
